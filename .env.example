# Base URL for image upload API (no trailing slash).
# When unset, stub uploader uses local blob URLs for development.
# VITE_UPLOAD_BASE_URL=http://localhost:8008

# Force stub uploader even when base URL is set
# VITE_IMAGE_UPLOAD_STUB=true
